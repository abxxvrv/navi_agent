{{ content }}

---

以上是一段智能体对话中的消息列表。现在给你一个任务：按照特定优先级和规则压缩这段对话上下文。

**压缩优先级（按顺序）：**

1. **当前任务状态**：现在正在处理什么
2. **错误与解决方案**：所有遇到的错误及其解决办法
3. **代码演进**：只保留最终可工作的版本，删除中间尝试
4. **系统上下文**：项目结构、依赖、环境配置
5. **设计决策**：架构选择及其理由
6. **待办事项**：未完成任务和已知问题

**压缩规则：**

* 必须保留：错误消息、堆栈跟踪、可工作的解决方案、当前任务
* 合并：将相似讨论合并为单个总结点
* 删除：冗余解释、失败尝试；但保留从中得到的经验教训
* 压缩：长代码块 → 只保留函数签名和关键逻辑

**特殊处理：**

* 对于代码：如果少于 20 行，保留完整版本；否则保留函数签名和关键逻辑
* 对于错误：保留完整错误消息和最终解决方案
* 对于讨论：只提取决策和行动项
* 永远不要包含 API key、token、密码、密钥、凭证 —— 用 `[REDACTED]` 替换

**你应该按如下输出结构输出：**

<current_focus>
[我们现在正在做什么 —— 原样复制用户最近一次请求]
</current_focus>

<environment>
- [关键设置/配置点]
- ……更多……
</environment>

<completed_tasks>

* [任务]：[简要结果]
* ……更多……
  </completed_tasks>

<active_issues>

* [问题]：[状态/下一步]
* ……更多……
  </active_issues>

<code_state>

<file>
[文件名]

**摘要：**
[这个代码文件的作用]

**关键元素：**

* [重要函数/类]
* ……更多……

**最新版本：**
[这个文件中的关键代码片段] </file>

<file>
[文件名]
……类似上面的格式……
</file>

……更多文件……
</code_state>

<important_context>

* [上面未覆盖的任何关键信息]
* ……更多……
</important_context>

要具体 —— 包括文件路径、命令输出、错误消息、行号和具体数值。避免使用“做了一些更改”之类含糊描述 —— 要说明具体改了什么。

只写总结正文。不要包含任何开场白或前缀。
